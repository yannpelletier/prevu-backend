
APP_NAME=PrevU
APP_ENV=local
APP_KEY=base64:INSERT RANDOM 32 CHARACTERS STRING
APP_DEBUG=true

API_URL=http://localhost:5000/api
FRONTEND_URL=http://localhost:8080
BACKEND_URL=http://localhost:5000
PUBLIC_STORAGE_URL=http://localhost:5000/storage/
DOMAIN=localhost

LOG_CHANNEL=stack

DB_CONNECTION=sqlite
DB_DATABASE=:memory:
SESSION_DOMAIN=localhost

GOOGLE_CLIENT_ID=INSERT CLIENT ID
GOOGLE_CLIENT_SECRET=INSERT CLIENT SECRET
GOOGLE_REDIRECT=http://localhost:8080/google-sign-in-callback

STRIPE_PUBLIC_KEY=INSERT PUBLIC KEY
STRIPE_SECRET_KEY=INSERT PRIVATE KEY
STRIPE_CLIENT_ID=INSERT CLIENT ID

CACHE_DRIVER=file
QUEUE_CONNECTION=database
SESSION_DRIVER=file
SESSION_LIFETIME=120

BROADCAST_DRIVER=pusher
PUSHER_APP_CLUSTER=us2
PUSHER_APP_ID=INSERT APP ID
PUSHER_APP_KEY=INSERT APP KEY
PUSHER_APP_SECRET=INSERT APP SECRET

MAIL_DRIVER=mailgun
MAILGUN_DOMAIN=INSERT DOMAIN
MAILGUN_SECRET=INSERT MAILGUN SECRET
MAIL_FROM_ADDRESS=INSERT FROM ADDRESS
MAILGUN_TEST_MODE=true

MAILCHIMP_APIKEY=INSERT API KEY
MAILCHIMP_LIST_ID=INSERT LIST ID

FILESYSTEM_DRIVER=s3
AWS_ACCESS_KEY_ID=INSERT ACCESS KEY ID
AWS_SECRET_ACCESS_KEY=INSERT ACCESS KEY
AWS_DEFAULT_REGION=us-east-2
AWS_BUCKET=INSERT AWS BUCKET
AWS_URL=INSERT AWS URL

BUGSNAG_API_KEY=INSERT API KEY

PASSPORT_PUBLIC_KEY=INSERT PUBLIC KEY

PASSPORT_PRIVATE_KEY=INSERT PRIVATE KEY

